/*#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <math.h>

// includes, kernels
#include "matmul.h"


void matmul(float*, const float*, const float*, unsigned int, unsigned int, unsigned int);
*/
/*int mabain() {
  Matrix M;
  Matrix N;
  Matrix P;
  for (int a = 0; a < 16; a++){
    M.elements[a] = a + 1;
  }
  for (int a = 0; a < 16; a++){
    N.elements[a] = 16 - a;
  }
  matmul(P.elements, M.elements, N.elements, 4, 4, 4);
  for (int a = 0; a < 16; a++){
    fprintf(stdout, "%d", int(P.elements[a]));
  }
}*/
